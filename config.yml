# config.yaml
paths:
  data_dir: "data"
  raw_dir: "data/raw"
  raw_parquet_path: "data/consolidated.parquet"
  staging_duckdb_path: "data/sensors.duckdb"
  notebooks_dir: "notebooks"
  dbt_dir: "dbt"

collection:
  start_datetime: "2020-11-01T00:00:00Z"
  end_datetime: null # null = collect up to now, otherwise choose an end date
  delta_hours: 1 # fixed by convention; see note below

requests:
  api_url: "https://api.airelib.re/api/v1/aqi"
  max_retries: 3
  backoff_base_seconds: 2
  timeout_seconds: 5

staging:
  coord_decimal_precision: 4
  coord_tolerance: 0.01 # ~1km of jitter is allowed in sensor location
