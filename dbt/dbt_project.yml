name: 'airelibre_analytics'
version: '1.0.0'

profile: 'airelibre_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]

clean-targets:
  - "target"
  - "dbt_packages"
  
vars:
  delta_dt_h: 1
  loc_asucentro:
    lat: -25.2968
    lon: -57.6350
  valid_params:
    min_readings: 24*60
    min_coverage_frac: 0.5

models:
  airelibre_analytics:
    intermediate:
      +materialized: table
    marts:
      +materialized: table
