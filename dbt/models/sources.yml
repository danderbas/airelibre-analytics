sources:
  - name: core
    schema: core
    tables:
      - name: located_sensors
        columns:
          - name: located_sensor_id
            tests: [unique, not_null]
      - name: readings
        columns:
          - name: lsid
            tests:
              - not_null
              - relationships:
                  arguments:
                    to: source('core', 'located_sensors')
                    field: located_sensor_id
                  config:
                    severity: error
          - name: dt
            tests: [not_null]
          - name: aqi
            tests: [not_null]